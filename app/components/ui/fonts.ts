import { Comfortaa, Open_Sans, Raleway } from "next/font/google";

export const comfortaaFont = Comfortaa({ subsets: ["latin"], weight: ["400", "700"], });
export const openSansFont = Open_Sans({ subsets: ["latin"] });
export const ralewayFont = Raleway({ subsets: ["latin"] });